services:
  - type: web
    name: statcan-mcp-server
    runtime: python
    buildCommand: pip install .
    startCommand: statcan-mcp-server --transport http
    envVars:
      - key: STATCAN_TRANSPORT
        value: http
      - key: PORT
        generateValue: false
      - key: RENDER_BASE_URL
        value: https://mcp-statcan.onrender.com
    healthCheckPath: /health
    plan: free
